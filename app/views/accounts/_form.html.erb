<%= bootstrap_form_for(account) do |f| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :role, disabled: true %>
  </div>

  <div class="field">
    <%= f.text_field :state, disabled: false %>
  </div>

  <div class="field">
    <%= f.text_field :category, value: "turismo", disabled: true %>
  </div>

  <div class="field">
    <%= f.text_field :location, value: "pereira", disabled: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
